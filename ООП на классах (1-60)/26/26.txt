1.
Этот код создает новый объект RegExp без указания шаблона поиска. В консоль будет выведен объект RegExp, и console.dir покажет его свойства и методы. Проверка reg instanceof RegExp вернет true, подтверждая, что reg является экземпляром класса RegExp.

2.
Здесь создается массив с элементами 1, 2, 3. В консоль будет выведен массив, и console.dir покажет его свойства и методы. Проверка arr instanceof Array вернет true, подтверждая, что arr является экземпляром класса Array.

3.
Аналогично предыдущему коду, создается массив с элементами 1, 2, 3, но с использованием конструктора Array. Результаты будут такими же, как и в предыдущем примере.

4.
Создается объект с тремя свойствами. В консоль будет выведен объект, и console.dir покажет его свойства и методы. Проверка obj instanceof Object вернет true, подтверждая, что obj является экземпляром класса Object.

5.
Создается новый пустой объект с использованием конструктора Object. В консоль будет выведен объект, и console.dir покажет его свойства и методы. Проверка obj instanceof Object вернет true, подтверждая, что obj является экземпляром класса Object.